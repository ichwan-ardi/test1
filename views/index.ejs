<!DOCTYPE html>
<html lang="id">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <!-- Meta SEO & Preview -->
    <meta name="description" content="Guyu Chat — Aplikasi chat aman dengan privasi tinggi, cepat, dan seru!" />
    <meta property="og:title" content="Guyu Chat - Ngobrol Bebas, Gak Ada Ngintip!" />
    <meta property="og:description" content="Cara lebih baik untuk terhubung dan berkomunikasi dengan privasi." />
    <meta property="og:image" content="/icon/logo.png" />
    <meta property="og:type" content="website" />
    <link rel="icon" type="image/png" href="/icon/logo.png" />
    <link rel="stylesheet" href="/src/output.css" />
    <title>Guyu Chat</title>
    <!-- AOS Animation -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/aos/2.3.4/aos.css" />
    <!-- Fonts -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/inter-ui/3.19.3/inter.min.css" />

    <!-- Custom Styles -->
    <style>
      :root {
        --primary-color: #000000;
        --accent-color: #3b82f6;
        --text-primary: #1e293b;
        --text-secondary: #64748b;
        --bg-color: #ffffff;
        --bg-secondary: #f9fafb;
        --transition-speed: 0.3s;
        --border-radius: 16px;
      }

      body {
        font-family: 'Inter', ui-sans-serif, system-ui, -apple-system, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, 'Noto Sans', sans-serif;
        background-color: var(--bg-color);
        color: var(--text-primary);
        overflow-x: hidden;
      }

      /* Modern Glass Effect */
      .glass-effect {
        background: rgba(255, 255, 255, 0.85);
        backdrop-filter: blur(12px);
        -webkit-backdrop-filter: blur(12px);
        border: 1px solid rgba(255, 255, 255, 0.18);
      }

      /* Button Styles */
      .btn-hover {
        transition: all var(--transition-speed) ease-in-out;
      }

      .btn-hover:hover {
        transform: scale(1.05);
        box-shadow: 0 10px 20px rgba(0, 0, 0, 0.2);
      }

      /* Beautiful gradient for accent */
      .accent-gradient {
        background: linear-gradient(90deg, #3b82f6, #8b5cf6);
        -webkit-background-clip: text;
        background-clip: text;
        color: transparent;
        font-weight: 700;
      }

      /* Modern Button with gradient */
      .btn-gradient {
        background: linear-gradient(90deg, #3b82f6, #8b5cf6);
        color: white;
        border: none;
        position: relative;
        z-index: 1;
      }

      .btn-gradient::before {
        content: '';
        position: absolute;
        top: 0;
        right: 0;
        bottom: 0;
        left: 0;
        background: linear-gradient(90deg, #1d4ed8, #7c3aed);
        border-radius: inherit;
        z-index: -1;
        transition: opacity 0.3s ease-in-out;
        opacity: 0;
      }

      .btn-gradient:hover::before {
        opacity: 1;
      }

      /* Modern hamburger */
      #hamburger {
        outline: none;
        -webkit-tap-highlight-color: transparent;
      }

      #hamburger:focus {
        outline: none;
        box-shadow: none;
      }

      .hamburger-line {
        width: 24px;
        height: 2px;
        display: block;
        transition: all 0.3s ease-in-out;
      }

      .hamburger-active .hamburger-line:nth-child(1) {
        transform: translateY(8px) rotate(45deg);
      }

      .hamburger-active .hamburger-line:nth-child(2) {
        transform: translateY(0) rotate(-45deg);
      }

      #navLink {
        display: none;
        position: absolute;
        top: 80px;
        right: 20px;
        z-index: 50;
        background-color: var(--bg-color);
        box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.1);
        border-radius: var(--border-radius);
        animation: slideDown 0.3s ease-out forwards;
      }

      #navLink.active {
        display: flex;
      }

      /* Modern animations */
      @keyframes slideDown {
        from {
          opacity: 0;
          transform: translateY(-10px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }

      @keyframes floating {
        0% {
          transform: translateY(0px);
        }
        50% {
          transform: translateY(-15px);
        }
        100% {
          transform: translateY(0px);
        }
      }

      .floating-animation {
        animation: floating 4s ease-in-out infinite;
      }

      /* Background gradient pattern */
      .bg-pattern {
        background-image: radial-gradient(circle at 30% 20%, rgba(59, 130, 246, 0.1) 0%, transparent 50%), radial-gradient(circle at 80% 70%, rgba(139, 92, 246, 0.1) 0%, transparent 50%);
        background-size: 100% 100%;
        background-position: center;
      }

      /* Responsive Typography */
      @media (max-width: 640px) {
        .responsive-heading {
          font-size: 2.5rem;
          line-height: 1.2;
        }
        .responsive-text {
          font-size: 1rem;
          line-height: 1.5;
        }
      }

      /* Chat Bubbles */
      .chat-bubble {
        padding: 12px 18px;
        border-radius: 20px;
        position: relative;
        margin-bottom: 8px;
        max-width: 80%;
        box-shadow: 0 1px 2px rgba(0, 0, 0, 0.1);
      }

      .chat-bubble.left {
        background-color: #f1f5f9;
        border-bottom-left-radius: 4px;
        align-self: flex-start;
      }

      .chat-bubble.right {
        background: linear-gradient(90deg, #3b82f6, #8b5cf6);
        color: white;
        border-bottom-right-radius: 4px;
        align-self: flex-end;
      }
    </style>
  </head>

  <body class="bg-pattern">
    <!-- Navbar -->
    <header id="header" class="sticky top-0 z-40 transition-all duration-300">
      <div class="container mx-auto shadow-lg rounded-2xl flex items-center justify-between w-full max-w-6xl my-4 px-5 py-3" id="header-container">
        <a href="/" class="flex items-center">
          <img src="/icon/logo.png" alt="Guyu Chat Logo" class="h-12 md:h-16" loading="lazy" />
        </a>
        <!-- Desktop Navigation -->
        <nav class="items-center gap-4 hidden md:flex">
          <% if (user) { %>
          <a href="/chat" class="px-4 py-2 text-black font-medium hover:text-blue-500 transition-colors">Mulai Chat</a>
          <a href="/auth/logout" class="px-5 py-2 rounded-full text-white bg-black font-medium btn-hover transition-all">Logout</a>
          <% } else { %>
          <a href="/auth/login" class="px-4 py-2 text-black font-medium hover:text-blue-500 transition-colors">Login</a>
          <a href="/auth/register" class="px-5 py-2 rounded-full text-white bg-black font-medium btn-hover transition-all">Register</a>
          <% } %>
        </nav>
        <!-- Mobile Hamburger Menu -->
        <button id="hamburger" aria-label="Toggle menu" class="flex flex-col items-center justify-center space-y-2 cursor-pointer z-50 outline-0 md:hidden">
          <span class="hamburger-line transition duration-300 ease-in-out bg-black"></span>
          <span class="hamburger-line transition duration-300 ease-in-out bg-black"></span>
        </button>
      </div>
      <!-- Mobile Navigation Menu -->
      <div id="navLink" class="flex flex-col items-center space-y-3 px-6 py-4 rounded-xl w-[200px] glass-effect md:hidden">
        <% if (user) { %>
        <a href="/chat" class="w-full text-center px-4 py-2 text-black font-medium hover:text-blue-500 transition-colors">Mulai Chat</a>
        <a href="/auth/logout" class="w-full text-center px-4 py-2 rounded-full text-white bg-black font-medium btn-hover transition-all">Logout</a>
        <% } else { %>
        <a href="/auth/login" class="w-full text-center px-4 py-2 text-black font-medium hover:text-blue-500 transition-colors">Login</a>
        <a href="/auth/register" class="w-full text-center px-4 py-2 rounded-full text-white bg-black font-medium btn-hover transition-all">Register</a>
        <% } %>
      </div>
    </header>

    <!-- Main Section -->
    <main id="main" class="min-h-screen">
      <div class="container mx-auto flex flex-col items-center justify-center px-4 py-10">
        <!-- Announcement Banner -->
        <a href="#" class="flex items-center gap-2 glass-effect rounded-2xl p-2 shadow-md hover:shadow-xl transition w-fit mt-5 md:mt-20 mb-6" data-aos="fade-up" data-aos-duration="700" data-aos-delay="300">
          <span class="bg-black text-white rounded-full px-3 py-1 text-sm font-semibold">New</span>
          <span class="text-slate-600 text-sm font-medium pr-2">Guyu Chat Beta is now live! 🎉</span>
        </a>

        <!-- Hero Section -->
        <div class="text-center max-w-4xl">
          <h1 class="responsive-heading font-bold text-4xl sm:text-5xl md:text-6xl lg:text-7xl tracking-tight" data-aos="fade-down" data-aos-duration="700">
            Ngobrol Bebas,<br />
            <span class="accent-gradient">Gak Ada Ngintip!</span>
          </h1>

          <!-- Description -->
          <p class="responsive-text mt-6 text-lg md:text-xl text-slate-600 font-medium leading-relaxed max-w-2xl mx-auto" data-aos="fade-up" data-aos-duration="700" data-aos-delay="150">
            Cara lebih baik untuk terhubung dan berkomunikasi dengan privasi.<br class="hidden sm:block" />
            Pesan Anda, privasi Anda, kendali di tangan Anda.
          </p>

          <!-- CTA Button -->
          <a href="/chat" class="inline-flex items-center gap-2 btn-gradient rounded-full px-8 py-4 mt-8 text-lg font-medium transition-all btn-hover" data-aos="fade-up" data-aos-duration="700" data-aos-delay="250">
            <span>🚀 Mulai Sekarang!</span>
          </a>
        </div>

        <!-- Chat Demo Preview -->
        <div class="w-full max-w-md mt-16 glass-effect rounded-2xl p-6 shadow-xl" data-aos="fade-up" data-aos-duration="700" data-aos-delay="400">
          <h3 class="text-lg font-semibold mb-4 text-center">Preview Chat</h3>
          <div class="flex flex-col">
            <div class="chat-bubble left" data-aos="fade-left" data-aos-duration="700" data-aos-delay="480">Halo! Apa kabar?</div>
            <div class="chat-bubble right" data-aos="fade-right" data-aos-duration="700" data-aos-delay="520">Baik! Senang bisa chat aman di Guyu Chat.</div>
            <div class="chat-bubble left" data-aos="fade-left" data-aos-duration="700" data-aos-delay="540">Ya, gak perlu khawatir tentang privasi di sini 👍</div>
            <div class="chat-bubble right" data-aos="fade-right" data-aos-duration="700" data-aos-delay="560">Benar sekali! Semua pesan terjamin keamanannya 🔒</div>
          </div>
        </div>

        <!-- Features Section -->
        <div class="grid grid-cols-1 md:grid-cols-3 gap-8 w-full max-w-5xl mt-20" data-aos="fade-up" data-aos-duration="700" data-aos-delay="450">
          <div class="glass-effect p-6 rounded-2xl shadow-lg">
            <div class="text-3xl mb-4">🔒</div>
            <h3 class="text-xl font-bold mb-2">Enkripsi Tingkat Tinggi</h3>
            <p class="text-slate-600">Pesan Anda terlindungi dengan enkripsi end-to-end terbaik.</p>
          </div>
          <div class="glass-effect p-6 rounded-2xl shadow-lg">
            <div class="text-3xl mb-4">⚡</div>
            <h3 class="text-xl font-bold mb-2">Super Cepat</h3>
            <p class="text-slate-600">Pengiriman pesan instan dengan kecepatan tinggi.</p>
          </div>
          <div class="glass-effect p-6 rounded-2xl shadow-lg">
            <div class="text-3xl mb-4">🎨</div>
            <h3 class="text-xl font-bold mb-2">Desain Modern</h3>
            <p class="text-slate-600">Interface yang simpel namun elegan untuk pengalaman pengguna terbaik.</p>
          </div>
        </div>
      </div>
    </main>

    <!-- Footer -->
    <footer class="mt-20 py-8 glass-effect">
      <div class="container mx-auto px-4">
        <div class="flex flex-col items-center">
          <p class="text-sm text-slate-600 mt-2">© 2025 Guyu Chat. All rights reserved.</p>
        </div>
      </div>
    </footer>

    <script>
      // Toggle mobile menu
      document.addEventListener('DOMContentLoaded', function () {
        const hamburger = document.getElementById('hamburger');
        const navLink = document.getElementById('navLink');

        hamburger.addEventListener('click', function () {
          navLink.classList.toggle('active');
          hamburger.classList.toggle('hamburger-active');
        });

        // Close menu when clicking outside
        document.addEventListener('click', function (event) {
          if (!hamburger.contains(event.target) && !navLink.contains(event.target)) {
            navLink.classList.remove('active');
            hamburger.classList.remove('hamburger-active');
          }
        });
      });

      // nav blur
      document.addEventListener('DOMContentLoaded', function () {
        const header = document.getElementById('header');
        const headerContainer = document.getElementById('header-container');

        window.addEventListener('scroll', function () {
          if (window.scrollY > 10) {
            header.classList.add('backdrop-blur-md');
            headerContainer.classList.add('bg-opacity-80');
          } else {
            header.classList.remove('backdrop-blur-md');
            headerContainer.classList.remove('bg-opacity-80');
          }
        });
      });
    </script>

    <!-- AOS Animation Script -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/aos/2.3.4/aos.js"></script>
    <script>
      AOS.init({
        once: true,
        disable: 'phone',
      });
    </script>
  </body>
</html>
